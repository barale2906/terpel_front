/**
 * Configuración global de Vitest.
 *
 * Importa los matchers extendidos de jest-dom para poder usar
 * aserciones como toBeInTheDocument(), toHaveAttribute(), etc.
 * en los tests de componentes React.
 */
import '@testing-library/jest-dom'
